#ifndef FUCTION_H
    #define FUNCTION_H

    struct timeval convertTime(struct timeval, struct timeval);              // Função que calcula o tempo final.
    double convertTimeDouble(struct timeval, struct timeval);                // Função que converte a estrutura timeval em double.

#endif
